/** Gerado por npm run blade-console:embed — não editar manualmente. */
export const BLADE_CONSOLE_HTML = "<!DOCTYPE html>\r\n<html lang=\"pt\">\r\n<head>\r\n  <meta charset=\"UTF-8\" />\r\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\" />\r\n  <title>KiamiCloud — Consola Blade</title>\r\n  <style>\r\n    :root {\r\n      --bg: #0f1724;\r\n      --card: #162536;\r\n      --border: #243447;\r\n      --text: #e8eef5;\r\n      --muted: #8fa3b8;\r\n      --accent: #3b82f6;\r\n      --ok: #22c55e;\r\n      --warn: #f59e0b;\r\n    }\r\n    * { box-sizing: border-box; }\r\n    body {\r\n      margin: 0;\r\n      font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;\r\n      background: var(--bg);\r\n      color: var(--text);\r\n      min-height: 100vh;\r\n    }\r\n    header {\r\n      padding: 16px 24px;\r\n      border-bottom: 1px solid var(--border);\r\n      display: flex;\r\n      align-items: center;\r\n      justify-content: space-between;\r\n      gap: 12px;\r\n      flex-wrap: wrap;\r\n    }\r\n    header h1 { margin: 0; font-size: 1.15rem; font-weight: 600; }\r\n    header p { margin: 4px 0 0; color: var(--muted); font-size: 0.85rem; }\r\n    .badge {\r\n      font-size: 0.75rem;\r\n      padding: 4px 10px;\r\n      border-radius: 999px;\r\n      background: #1e3a5f;\r\n      color: #93c5fd;\r\n    }\r\n    main { padding: 20px 24px 40px; max-width: 1200px; margin: 0 auto; }\r\n    .grid {\r\n      display: grid;\r\n      grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));\r\n      gap: 12px;\r\n      margin-bottom: 20px;\r\n    }\r\n    .stat {\r\n      background: var(--card);\r\n      border: 1px solid var(--border);\r\n      border-radius: 10px;\r\n      padding: 14px;\r\n    }\r\n    .stat label { display: block; font-size: 0.75rem; color: var(--muted); }\r\n    .stat strong { font-size: 1.4rem; }\r\n    .panel {\r\n      background: var(--card);\r\n      border: 1px solid var(--border);\r\n      border-radius: 10px;\r\n      margin-bottom: 16px;\r\n      overflow: hidden;\r\n    }\r\n    .panel h2 {\r\n      margin: 0;\r\n      padding: 12px 16px;\r\n      font-size: 0.95rem;\r\n      border-bottom: 1px solid var(--border);\r\n    }\r\n    table { width: 100%; border-collapse: collapse; font-size: 0.85rem; }\r\n    th, td { padding: 10px 14px; text-align: left; border-bottom: 1px solid var(--border); }\r\n    th { color: var(--muted); font-weight: 500; font-size: 0.75rem; text-transform: uppercase; }\r\n    tr:last-child td { border-bottom: none; }\r\n    .muted { color: var(--muted); }\r\n    .login {\r\n      max-width: 380px;\r\n      margin: 80px auto;\r\n      padding: 24px;\r\n      background: var(--card);\r\n      border: 1px solid var(--border);\r\n      border-radius: 12px;\r\n    }\r\n    .login h1 { margin: 0 0 8px; font-size: 1.25rem; }\r\n    .login p { color: var(--muted); font-size: 0.9rem; margin: 0 0 20px; }\r\n    button {\r\n      background: var(--accent);\r\n      color: white;\r\n      border: none;\r\n      border-radius: 8px;\r\n      padding: 10px 16px;\r\n      font-size: 0.9rem;\r\n      cursor: pointer;\r\n      width: 100%;\r\n    }\r\n    button.secondary { background: transparent; border: 1px solid var(--border); color: var(--text); margin-top: 8px; }\r\n    button:disabled { opacity: 0.6; cursor: not-allowed; }\r\n    .error { color: #f87171; font-size: 0.85rem; margin-top: 10px; }\r\n    .login-field {\r\n      width: 100%;\r\n      margin-bottom: 12px;\r\n      padding: 10px 12px;\r\n      border-radius: 8px;\r\n      border: 1px solid var(--border);\r\n      background: var(--bg);\r\n      color: var(--text);\r\n      font-size: 0.95rem;\r\n    }\r\n    .login-field:focus { outline: 2px solid var(--accent); outline-offset: 1px; }\r\n    .toolbar { display: flex; gap: 8px; align-items: center; flex-wrap: wrap; }\r\n    .toolbar button { width: auto; }\r\n    .live { width: 8px; height: 8px; border-radius: 50%; background: var(--ok); display: inline-block; margin-right: 6px; }\r\n    .user-block { border-bottom: 1px solid var(--border); }\r\n    .user-block:last-child { border-bottom: none; }\r\n    .user-head {\r\n      padding: 12px 16px;\r\n      display: flex;\r\n      justify-content: space-between;\r\n      align-items: center;\r\n      gap: 12px;\r\n      flex-wrap: wrap;\r\n      background: rgba(255,255,255,0.02);\r\n      cursor: pointer;\r\n      user-select: none;\r\n    }\r\n    .user-head:hover { background: rgba(255,255,255,0.04); }\r\n    .user-head strong { font-size: 0.95rem; }\r\n    .user-meta { font-size: 0.8rem; color: var(--muted); }\r\n    .cat {\r\n      display: inline-block;\r\n      font-size: 0.7rem;\r\n      padding: 2px 8px;\r\n      border-radius: 999px;\r\n      background: #1e293b;\r\n      color: #94a3b8;\r\n      text-transform: capitalize;\r\n    }\r\n    .files-wrap { overflow-x: auto; }\r\n    .files-wrap[hidden] { display: none; }\r\n  </style>\r\n</head>\r\n<body>\r\n  <div id=\"login-view\" class=\"login\">\r\n    <h1>Consola Blade</h1>\r\n    <p>Monitorização local (LAN). Dados do ZimaBlade apenas.</p>\r\n    <form id=\"login-form\">\r\n      <input id=\"username-input\" class=\"login-field\" type=\"text\" autocomplete=\"username\" value=\"admin\" placeholder=\"Utilizador\" required />\r\n      <input id=\"password-input\" class=\"login-field\" type=\"password\" autocomplete=\"current-password\" placeholder=\"Password\" required />\r\n      <button id=\"btn-login\" type=\"submit\">Entrar</button>\r\n    </form>\r\n    <div id=\"login-error\" class=\"error\"></div>\r\n  </div>\r\n\r\n  <div id=\"app-view\" hidden>\r\n    <header>\r\n      <div>\r\n        <h1><span class=\"live\"></span>KiamiCloud — ZimaBlade</h1>\r\n        <p id=\"admin-label\" class=\"muted\">Gestão local</p>\r\n      </div>\r\n      <div class=\"toolbar\">\r\n        <span class=\"badge\">API local · LAN</span>\r\n        <button id=\"btn-refresh\" type=\"button\">Actualizar</button>\r\n        <button id=\"btn-logout\" class=\"secondary\" type=\"button\">Sair</button>\r\n      </div>\r\n    </header>\r\n    <main>\r\n      <div class=\"grid\" id=\"stats\"></div>\r\n      <div class=\"panel\">\r\n        <h2>Ficheiros por utilizador</h2>\r\n        <div id=\"files-by-user\"></div>\r\n      </div>\r\n      <div class=\"panel\">\r\n        <h2>Utilizadores activos (últimos 30 min)</h2>\r\n        <div style=\"overflow-x:auto\">\r\n          <table><thead><tr><th>Utilizador</th><th>UID</th><th>Ficheiros</th><th>Última actividade</th></tr></thead><tbody id=\"active-users\"></tbody></table>\r\n        </div>\r\n      </div>\r\n      <div class=\"panel\">\r\n        <h2>Actividade recente</h2>\r\n        <div style=\"overflow-x:auto\">\r\n          <table><thead><tr><th>Hora</th><th>Utilizador</th><th>Acção</th><th>Ficheiro</th></tr></thead><tbody id=\"activity\"></tbody></table>\r\n        </div>\r\n      </div>\r\n      <div class=\"panel\">\r\n        <h2>Eventos de segurança</h2>\r\n        <div style=\"overflow-x:auto\">\r\n          <table><thead><tr><th>Hora</th><th>Tipo</th><th>UID</th><th>Path</th></tr></thead><tbody id=\"security\"></tbody></table>\r\n        </div>\r\n      </div>\r\n    </main>\r\n  </div>\r\n\r\n  <script>\r\n    const API = window.location.origin;\r\n    let poll;\r\n\r\n    const $ = (id) => document.getElementById(id);\r\n    const loginView = $('login-view');\r\n    const appView = $('app-view');\r\n    const loginError = $('login-error');\r\n    const usernameInput = $('username-input');\r\n    const passwordInput = $('password-input');\r\n\r\n    async function api(path, options = {}) {\r\n      const res = await fetch(API + path, {\r\n        credentials: 'include',\r\n        ...options,\r\n        headers: {\r\n          Accept: 'application/json',\r\n          ...(options.headers || {}),\r\n        },\r\n      });\r\n      const body = await res.json().catch(() => ({}));\r\n      if (!res.ok) throw new Error(body.message || 'Erro ' + res.status);\r\n      return body;\r\n    }\r\n\r\n    function showLogin() {\r\n      clearInterval(poll);\r\n      loginView.hidden = false;\r\n      appView.hidden = true;\r\n    }\r\n\r\n    function showApp(user) {\r\n      loginView.hidden = true;\r\n      appView.hidden = false;\r\n      loginError.textContent = '';\r\n      $('admin-label').textContent = user ? `Sessão: ${user}` : 'Gestão local';\r\n    }\r\n\r\n    function fmtDate(iso) {\r\n      if (!iso) return '—';\r\n      try { return new Date(iso + 'Z').toLocaleString('pt-PT'); } catch { return iso; }\r\n    }\r\n\r\n    function esc(s) {\r\n      return String(s ?? '').replace(/[&<>\"']/g, (c) =>\r\n        ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;', \"'\": '&#39;' }[c]));\r\n    }\r\n\r\n    function fmtBytes(n) {\r\n      const b = Number(n) || 0;\r\n      if (b < 1024) return b + ' B';\r\n      if (b < 1024 * 1024) return (b / 1024).toFixed(1) + ' KB';\r\n      if (b < 1024 * 1024 * 1024) return (b / 1024 / 1024).toFixed(1) + ' MB';\r\n      return (b / 1024 / 1024 / 1024).toFixed(2) + ' GB';\r\n    }\r\n\r\n    function userLabel(u) {\r\n      return u.displayName || u.email || u.uid;\r\n    }\r\n\r\n    async function loadDashboard() {\r\n      const [statsRes, filesRes, activeRes, feedRes, secRes] = await Promise.all([\r\n        api('/blade-console/api/stats'),\r\n        api('/blade-console/api/files-by-user?limitPerUser=200'),\r\n        api('/blade-console/api/activity/active-users?minutes=30'),\r\n        api('/blade-console/api/activity/feed?limit=60'),\r\n        api('/blade-console/api/security-events?limit=30'),\r\n      ]);\r\n\r\n      const s = statsRes.stats;\r\n      $('stats').innerHTML = [\r\n        ['Utilizadores', s.usersCount],\r\n        ['Ficheiros activos', s.activeFilesCount],\r\n        ['Armazenamento', Math.round(s.totalStorageUsedBytes / 1024 / 1024) + ' MB'],\r\n        ['Eventos 24h', s.securityEventsLast24h],\r\n      ].map(([label, val]) =>\r\n        `<div class=\"stat\"><label>${esc(label)}</label><strong>${esc(val)}</strong></div>`\r\n      ).join('');\r\n\r\n      const filesRoot = $('files-by-user');\r\n      const groups = filesRes.users || [];\r\n      if (!groups.length) {\r\n        filesRoot.innerHTML = '<p class=\"muted\" style=\"padding:16px\">Sem ficheiros activos</p>';\r\n      } else {\r\n        filesRoot.innerHTML = groups.map((u, idx) => {\r\n          const label = esc(userLabel(u));\r\n          const shown = u.files.length;\r\n          const total = u.filesCount;\r\n          const trunc = shown < total ? ` (mostrando ${shown} de ${total})` : '';\r\n          const rows = u.files.map((f) =>\r\n            `<tr>\r\n              <td>${esc(f.name)}</td>\r\n              <td><span class=\"cat\">${esc(f.category)}</span></td>\r\n              <td>${esc(f.mimeType || '—')}</td>\r\n              <td>${fmtBytes(f.sizeBytes)}</td>\r\n              <td class=\"muted\">${fmtDate(f.createdAt)}</td>\r\n            </tr>`\r\n          ).join('');\r\n          return `<div class=\"user-block\">\r\n            <div class=\"user-head\" data-toggle=\"user-${idx}\">\r\n              <div>\r\n                <strong>${label}</strong>\r\n                <div class=\"user-meta\">${esc(u.uid).slice(0, 16)}… · ${total} ficheiro(s) · ${fmtBytes(u.totalSizeBytes)}${trunc}</div>\r\n              </div>\r\n              <span class=\"muted\">▼</span>\r\n            </div>\r\n            <div class=\"files-wrap\" id=\"user-${idx}\">\r\n              <table>\r\n                <thead><tr><th>Nome</th><th>Tipo</th><th>MIME</th><th>Tamanho</th><th>Criado</th></tr></thead>\r\n                <tbody>${rows || '<tr><td colspan=\"5\" class=\"muted\">—</td></tr>'}</tbody>\r\n              </table>\r\n            </div>\r\n          </div>`;\r\n        }).join('');\r\n        filesRoot.querySelectorAll('.user-head').forEach((head) => {\r\n          head.addEventListener('click', () => {\r\n            const id = head.getAttribute('data-toggle');\r\n            const panel = document.getElementById(id);\r\n            if (panel) panel.hidden = !panel.hidden;\r\n          });\r\n        });\r\n      }\r\n\r\n      $('active-users').innerHTML = (activeRes.users || []).map((u) =>\r\n        `<tr><td>${esc(u.displayName || u.email || '—')}</td><td class=\"muted\">${esc(u.uid).slice(0, 12)}…</td><td>${u.filesCount}</td><td>${fmtDate(u.lastSeenAt)}</td></tr>`\r\n      ).join('') || '<tr><td colspan=\"4\" class=\"muted\">Nenhum utilizador activo</td></tr>';\r\n\r\n      $('activity').innerHTML = (feedRes.feed || []).map((a) =>\r\n        `<tr><td>${fmtDate(a.createdAt)}</td><td>${esc(a.displayName || a.email || a.uid)}</td><td>${esc(a.action)}</td><td class=\"muted\">${esc(a.fileId || '—')}</td></tr>`\r\n      ).join('') || '<tr><td colspan=\"4\" class=\"muted\">Sem actividade</td></tr>';\r\n\r\n      $('security').innerHTML = (secRes.events || []).map((e) =>\r\n        `<tr><td>${fmtDate(e.createdAt)}</td><td>${esc(e.eventType)}</td><td class=\"muted\">${esc(e.firebaseUid || '—')}</td><td class=\"muted\">${esc(e.path || '—')}</td></tr>`\r\n      ).join('') || '<tr><td colspan=\"4\" class=\"muted\">Sem eventos</td></tr>';\r\n    }\r\n\r\n    async function tryRestoreSession() {\r\n      try {\r\n        const session = await api('/blade-console/session');\r\n        showApp(session.user);\r\n        await loadDashboard();\r\n        poll = setInterval(() => loadDashboard().catch(handleAuthError), 15000);\r\n      } catch {\r\n        showLogin();\r\n      }\r\n    }\r\n\r\n    function handleAuthError(e) {\r\n      showLogin();\r\n      loginError.textContent = e.message || 'Sessao expirada. Entre novamente.';\r\n    }\r\n\r\n    $('login-form').addEventListener('submit', async (ev) => {\r\n      ev.preventDefault();\r\n      loginError.textContent = '';\r\n      try {\r\n        const session = await api('/blade-console/login', {\r\n          method: 'POST',\r\n          headers: { 'Content-Type': 'application/json' },\r\n          body: JSON.stringify({\r\n            username: usernameInput.value.trim(),\r\n            password: passwordInput.value,\r\n          }),\r\n        });\r\n        passwordInput.value = '';\r\n        showApp(session.user);\r\n        await loadDashboard();\r\n        poll = setInterval(() => loadDashboard().catch(handleAuthError), 15000);\r\n      } catch (e) {\r\n        loginError.textContent = e.message || 'Utilizador ou password incorrectos';\r\n      }\r\n    });\r\n\r\n    $('btn-logout').onclick = async () => {\r\n      try { await api('/blade-console/logout', { method: 'POST' }); } catch (_) { /* ignore */ }\r\n      showLogin();\r\n    };\r\n    $('btn-refresh').onclick = () => loadDashboard().catch((e) => {\r\n      loginError.textContent = e.message || String(e);\r\n    });\r\n\r\n    tryRestoreSession();\r\n  </script>\r\n</body>\r\n</html>\r\n";
