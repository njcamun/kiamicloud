# Workspace root — necessário para `dart run melos` (Melos 6.x + melos.yaml).
# O deploy CI não usa Melos; ver .github/workflows/deploy-web.yml.
name: kiamicloud_workspace
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  melos: ^6.3.2
