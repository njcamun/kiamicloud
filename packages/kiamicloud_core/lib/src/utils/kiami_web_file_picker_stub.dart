import 'package:file_picker/file_picker.dart';

/// Stub — apenas Web usa implementação HTML nativa.
Future<List<PlatformFile>?> pickFilesWithWebInput({
  bool allowMultiple = true,
}) async =>
    null;
